# GNUPLOT script to plot numerical solution vs. exact solution and compare different run types

# Screen output 
set terminal x11 persist
set key outside                        # put legend outside 
unset log                              # remove any log-scaling
unset label                            # remove any previous labels
set xtic auto                          # set xtics automatically
set ytic auto                          # set ytics automatically
set xrange [-5:5]
#set yrange [0.0:2e5]
set title "Sod shock tube: Density"
set xlabel "$x$"
set ylabel "$\\rho$"

plot    "<sort -gk 1 ./exact.dat"                         u 1:2 w p lw 1 lt 1 t 'Exact', \
        "<sort -gk 1 ./shocktube_FORWARD_EULER.dat"       u 1:2 w l lw 1 lt 1 t 'FORWARD_EULER', \
        "<sort -gk 1 ./shocktube_RK.dat"                  u 1:2 w l lw 1 lt 2 t 'RK', \
        "<sort -gk 1 ./shocktube_BDF2.dat"                u 1:2 w l lw 1 lt 3 t 'BDF2', \
        "<sort -gk 1 ./shocktube_BACKWARD_EULER.dat"      u 1:2 w l lw 1 lt 4 t 'BACKWARD_EULER'

# Output for latex
set nokey
set notitle
set terminal epslatex color
set output "shocktube_sod_comp.tex"
replot
